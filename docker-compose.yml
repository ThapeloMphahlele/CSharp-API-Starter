version: '3.8'

services:
  mysql-db:
    image: mysql:8.0
    container_name: barebones_mysql
    environment:
      MYSQL_ROOT_PASSWORD: barebonespassword
      MYSQL_DATABASE: BareBonesDb
    ports:
      - "3308:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: